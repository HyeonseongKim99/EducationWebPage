{
  "name": "education-web-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run content:prepare && docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "content:prepare": "node scripts/prepare-content.mjs",
    "check": "npm run content:prepare && npm run build && npm test",
    "test": "node --test"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.1",
    "@docusaurus/preset-classic": "3.10.1",
    "@mdx-js/react": "^3.1.1",
    "clsx": "^2.1.1",
    "gray-matter": "^4.0.3",
    "prism-react-renderer": "^2.4.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "engines": {
    "node": ">=20.0"
  }
}
